### 1. Create a new user for deletion test
POST http://localhost:8080/api/auth/register
Content-Type: application/json

{
  "username": "test.delete",
  "firstName": "Test",
  "lastName": "Delete",
  "birthDate": "1995-03-10",
  "password": "deleteTestPassword123"
}

### 2. Login with the created user to get JWT token
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "username": "test.delete",
  "password": "deleteTestPassword123"
}

> {%
    // Extract JWT token from login response
    if (response.status === 200) {
        client.global.set("jwt_token", response.body.token);
        client.log("JWT Token extracted: " + response.body.token.substring(0, 20) + "...");
    } else {
        client.log("Login failed: " + response.body.message);
    }
%}

### 3. Verify current user with token (optional - for verification)
GET http://localhost:8080/api/auth/me
Authorization: Bearer {{jwt_token}}

### 4. Delete the current user
DELETE http://localhost:8080/api/auth
Authorization: Bearer {{jwt_token}}

### 5. Try to access user info again (should fail after deletion)
GET http://localhost:8080/api/auth/me
Authorization: Bearer {{jwt_token}}

### 6. Try to login again with deleted user (should fail)
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "username": "test.delete",
  "password": "deleteTestPassword123"
}
